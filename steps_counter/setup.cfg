[metadata]
name = steps_counter
version = 0.1.0
description = ROS2 publisher for step counts (steps.json)

[options]
packages = find:
install_requires =
    rclpy
    std_msgs
zip_safe = false

[develop]
script_dir=$base/lib/steps_counter

[install]
install_scripts=$base/lib/steps_counter
